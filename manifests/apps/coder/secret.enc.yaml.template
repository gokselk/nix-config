# Coder database URL secret (encrypt this with sops)
#
# Setup:
# 1. Update the password to match your PostgreSQL secret
# 2. Rename to secret.enc.yaml
# 3. Encrypt: sops -e -i secret.enc.yaml
#
apiVersion: v1
kind: Secret
metadata:
  name: coder-db-url
  namespace: coder
type: Opaque
stringData:
  url: "postgres://coder:your-secure-password-here@pg-shared-rw.databases.svc.cluster.local:5432/coder?sslmode=require"
